# macOS
.DS_Store

# Claude Code local settings (personal, per-machine)
.claude/settings.local.json

# Xcode user-specific state
**/xcuserdata/
*.xcuserstate

# Build artifacts
/build/
*.xcarchive
*.ipa

# Local secrets (BUZZER_TOKEN for testing against the relay)
.env
